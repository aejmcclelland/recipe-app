//import global styles
import { Archivo, Shadows_Into_Light } from 'next/font/google';

export const archivo = Archivo({ subsets: ['latin'], display: 'swap' });

export const shadowsIntoLight = Shadows_Into_Light({
	subsets: ['latin'],
	display: 'swap',
	weight: '400',
});
